//An interface implmented by any UIElement whose size can change.
//Uses the event framework.
//UIEvents notifying of a changed dimension should pass in the generator as the event's data.

package org.haferlib.slick.ui;

public interface DynamicDimensions extends UIEventGenerator {

	
}